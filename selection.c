/*#include<stdio.h>

int main()
{
    int i,a[20],t,n,j;

    printf("Enter the no. of elements ");
    scanf("%d",&n);

    printf("Enter the elements");

    for(i=0;i<n;i++)
     scanf("%d",&a[i]);

    for(i=0;i<n;i++)
    {
        for(j=0;j<n;j++)
       if(a[i]<a[j])
        {
         t=a[i];
         a[i]=a[j];
         a[j]=t;
        }
    }


 printf("After the sorting\n");

     for(i=0;i<n;i++)
       printf("%d",a[i]);

return 0;

}
*/

