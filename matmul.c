/*#include<stdio.h>
#include<conio.h>
#include<malloc.h>

void main()
{
    int m1,n1,m2,n2;
    int **A,**B,**C;
    int i,j,e;

    printf("Enter the no. of rows and cols in the 1st matrix : ");
    scanf("%d%d"",&m1,&n1,&m1");
    printf("Enter the no. of rows and cols in the 2nd matrix : ");
    scanf("%d%d"",&m1,&n2,&m2");

    A=(int**)malloc(m1*sizeof(int*));
    for(i=0;i<n1;i++)
        A=(int*)malloc(n1*sizeof(int));

    B=(int**)malloc(m2*sizeof(int*));
    for(i=0;i<n2;i++)
        B=(int*)malloc(n2*sizeof(int));

    C=(int**)malloc(m1*sizeof(int*));
    for(i=0;i<n2;i++)
        C=(int**)malloc(m1*sizeof(int*));









}

*/
